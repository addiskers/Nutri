# Copy this file to .env and update the values

# MongoDB Configuration
MONGODB_URL=mongodb://localhost:27017
DATABASE_NAME=nutrieyeq

# JWT Secret Keys (Generate with: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=your-super-secret-jwt-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Email Configuration (Optional - for password reset)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
FROM_EMAIL=noreply@nutrieyeq.com
FROM_NAME=NutriEyeQ

# Frontend URL (for CORS and password reset links)
FRONTEND_URL=http://localhost:5173

# Security
RATE_LIMIT_PER_MINUTE=60
PASSWORD_RESET_TOKEN_EXPIRE_HOURS=24

# App Settings
APP_NAME=NutriEyeQ Dashboard
DEBUG=True

# Gemini AI Configuration (for product image extraction)
# Get your API key from: https://aistudio.google.com/app/apikey

