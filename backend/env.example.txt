# Copy this file to .env and update the values

# MongoDB Configuration
# For Docker: Use service name 'mongodb' as hostname
# Format: mongodb://username:password@host:port
MONGODB_URL=mongodb://admin:your-secure-password-here@mongodb:27017
DATABASE_NAME=nutrieyeq

# JWT Secret Keys (Generate with: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=your-super-secret-jwt-key-change-this-in-production
# During a SECRET_KEY rotation, set this to the previous key so currently
# active JWTs keep validating. Clear it after REFRESH_TOKEN_EXPIRE_DAYS has
# elapsed since the rotation.
# PREVIOUS_SECRET_KEY=
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# JWT claim identity. Both sides of decode assert these, so any mismatch
# between tokens minted here and tokens presented on requests produces an
# immediate 401. Override only if you run multiple deployments off the same
# signing key and need to partition them.
JWT_ISSUER=nutrieyeq-api
JWT_AUDIENCE=nutrieyeq-dashboard

# Email Configuration (Optional - for password reset)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
FROM_EMAIL=noreply@nutrieyeq.com
FROM_NAME=NutriEyeQ

# Frontend URL (for CORS and password reset links)
# For production: https://nutrieyeq.zyduswellness.com
FRONTEND_URL=https://nutrieyeq.zyduswellness.com

# Security
RATE_LIMIT_PER_MINUTE=60
PASSWORD_RESET_TOKEN_EXPIRE_HOURS=1

# Short-lived OTP windows
PASSWORD_RESET_OTP_EXPIRE_MINUTES=15
LOGIN_OTP_EXPIRE_MINUTES=10
OTP_MAX_ATTEMPTS=5
OTP_LOCKOUT_MINUTES=15

# AI extract upload caps and global body cap
MAX_UPLOAD_FILE_SIZE_MB=10
MAX_UPLOAD_TOTAL_SIZE_MB=50
MAX_REQUEST_BODY_SIZE_MB=100

# Per-user rate limit for Gemini-backed /products/extract and /coa/extract
EXTRACT_RATE_LIMIT=10/minute

# Optional registration email-domain allow-list. Comma-separated.
# Leave empty to allow any domain.
# ALLOWED_EMAIL_DOMAINS=zyduswellness.com,partner.example.com
ALLOWED_EMAIL_DOMAINS=

# Comma-separated list of emails auto-promoted to Super Admin on first login.
SUPER_ADMIN_EMAILS=

# App Settings
APP_NAME=NutriEyeQ Dashboard
# IMPORTANT: Set to False in production. DEBUG=True exposes /docs and allows all CORS origins.
DEBUG=False

# Gemini AI Configuration (for product image extraction)
# Get your API key from: https://aistudio.google.com/app/apikey

